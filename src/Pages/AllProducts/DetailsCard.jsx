/* eslint-disable react/prop-types */

const DetailsCard = ({ productDetail }) => {
  const {product_name, category_name, price, product_image, description} = productDetail || {};

  return (
    <>
      <section>
        <div></div>
      </section>
    </>
  );
};

export default DetailsCard;
