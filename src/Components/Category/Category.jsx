import useCategory from "../../Hooks/useCategory";

const Category = () => {
  const [allCategory] = useCategory();

  return (
    <>
      <div>
        
      </div>
    </>
  );
};

export default Category;
